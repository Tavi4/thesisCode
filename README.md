to activate env use: source .venv/bin/activate
to run test model: python3 -m scripts.test_model
to open isaac sim run in powershell: .\_build\windows-x86_64\release\isaac-sim.bat

to install what's needed:

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt



WITS:
python -m wits.part1Basic
...
python -m wits.part7WellTrained